## What does this change do, and why?

<!-- A short description. If it fixes a reported problem, link the issue below. -->

Fixes/relates to #

## Checklist

- [ ] Built and ran locally (`mvn -B package -P snapshot` at minimum)
- [ ] Scoped to one logical change — no unrelated fixes, refactors, or formatting bundled in
- [ ] Existing tests still pass
